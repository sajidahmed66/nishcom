
/*  
a slider component that displays a list of images
will do a loop through the images and display them
images will be displayed in a carousel
images links will be fatched from the database via axios call with restapi
*/
import { useState, useEffect } from "react";


const Slider = () => {
    return (
        <div  >
            <p>Array if images to be displayed here! </p>
        </div>
    );
}

export default Slider;